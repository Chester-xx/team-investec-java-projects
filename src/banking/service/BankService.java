// Holds all accounts in a List
// Provides findAccount(), openAccount(), closeAccount()
// Enforces ownership — customers can only access their own accounts

package banking.service;

public class BankService {
    
}
